// Ticker Banner Setup
document.addEventListener('DOMContentLoaded', function() {
    const tickerBanner = document.getElementById('ticker-banner');
    
    // Exit if ticker banner doesn't exist (not on homepage)
    if (!tickerBanner) {
        return;
    }
}); 